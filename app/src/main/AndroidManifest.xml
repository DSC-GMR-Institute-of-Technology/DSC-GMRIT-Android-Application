<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmrit.dscgmrit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Community_reg"></activity>
        <activity android:name=".IdeaSpot" />
        <activity android:name=".activities.FAQPage" />
        <activity android:name=".activities.AboutPage" />
        <activity android:name=".activities.EventDetailsActivity" />
        <activity android:name=".activities.SplashScreen" />
        <activity android:name=".activities.EventsActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>